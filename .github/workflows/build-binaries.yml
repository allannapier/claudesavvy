name: Build Release Binaries

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.11']
        include:
          - os: ubuntu-latest
            artifact_name: claude-monitor
            artifact_path: dist/claude-monitor
          - os: macos-latest
            artifact_name: claude-monitor-macos
            artifact_path: dist/claude-monitor
          - os: windows-latest
            artifact_name: claude-monitor.exe
            artifact_path: dist/claude-monitor.exe

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller -e .

      - name: Build executable with PyInstaller
        run: |
          pyinstaller claude-monitor.spec

      - name: Verify executable (Linux/macOS)
        if: runner.os != 'Windows'
        run: ./dist/claude-monitor --help

      - name: Verify executable (Windows)
        if: runner.os == 'Windows'
        run: dist\claude-monitor.exe --help

      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ matrix.artifact_path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
